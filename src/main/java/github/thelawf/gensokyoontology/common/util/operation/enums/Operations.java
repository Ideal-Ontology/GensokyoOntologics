package github.thelawf.gensokyoontology.common.util.operation.enums;

public enum Operations {
    ASSIGN,
    NEW_INSTANCE,
    REFERENCE,
    INVOKE,
    FOR_LOOP,
    IF_BRANCH,
    ELSE_BRANCH,
    ELSE_IF_BRANCH
}
